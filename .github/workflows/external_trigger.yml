name: External Trigger Main

on:
  workflow_dispatch:

jobs:
  external-trigger-alpine-i3:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.1.0

      - name: External Trigger
        if: github.ref == 'refs/heads/alpine-i3'
        run: |
          echo "**** No external release, exiting ****"
          exit 0
